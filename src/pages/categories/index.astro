---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { getCollection } from 'astro:content';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';

const posts = await getCollection('blog');
const categories = Array.from(new Set(posts.map(p => p.data.category).filter(Boolean))).sort();
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={`Categories · ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
  </head>
  <body>
    <Header />
    <main>
      <h1>Categories</h1>
      <ul>
        {categories.map(cat => (
          <li><a href={`/categories/${encodeURIComponent(cat!)}/`}>{cat}</a></li>
        ))}
      </ul>
    </main>
    <Footer />
  </body>
</html>

